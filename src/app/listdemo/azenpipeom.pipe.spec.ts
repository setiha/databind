import { AzenpipeomPipe } from './azenpipeom.pipe';

describe('AzenpipeomPipe', () => {
  it('create an instance', () => {
    const pipe = new AzenpipeomPipe();
    expect(pipe).toBeTruthy();
  });
});
